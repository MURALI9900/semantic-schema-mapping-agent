server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: semantic-agent
          uri: http://semantic-agent-service:8086
          predicates:
            - Path=/api/v1/semantic/**
